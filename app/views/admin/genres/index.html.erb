<h1>ジャンル一覧・追加</h1>

<%= form_for(@genre) do |f| %>
	<p>ジャンル</p>
	<%= f.text_field :name %>

	<%= f.submit '追加', class: "btn btn-primary col-xs-12" %>
 <% end %>

 <table>
	<thead>
		<tr>
			<th>ジャンル</th>
			<th>状態</th>
			<th></th>
	    </tr>
    </thead>
    <tbody>
       <% @genres.each do |genre| %>
		 	<tr>
		 		<td>
		 			<%#= link_to user_path(book.user) do %>
		 		</td>
		 		<td><%#= book.title %>
		 			<%= genre.name %>
		 		</td>
		 	    <td>
		 	    	
		 	    </td>
		 	    <td>
		 	    	<%= link_to 編集する, admin_genres_path %>
		 	    </td>
		 	</tr>
	  <% end %>
    </tbody>
 </table>